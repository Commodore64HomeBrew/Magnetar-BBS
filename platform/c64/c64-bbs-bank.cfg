# Bank overlay: fixed header/entries at $B000-$B014, code at $B015+.
FEATURES {
    STARTADDRESS: default = $B000;
}
SYMBOLS {
    __STACKSIZE__: type = export, value = $0200;
    __BANKMEMTOP__: type = export, value = $D000;
}
MEMORY {
    ZP:       file = "",   define = yes, start = $0002, size = $001A;
    BANKHDR:  file = %O,   start = $B000, size = $0015;
    RAM:      file = %O,   define = yes, start = $B015, size = __BANKMEMTOP__ - __STACKSIZE__ - $B015;
    STACK:    file = "",   define = yes, start = __BANKMEMTOP__ - __STACKSIZE__, size = __STACKSIZE__;
}
SEGMENTS {
    ZEROPAGE: load = ZP,       type = zp;
    BANKHDR:  load = BANKHDR,  type = ro,  define = yes;
    CODE:     load = RAM,      type = ro;
    RODATA:   load = RAM,      type = ro;
    DATA:     load = RAM,      type = rw;
    BSS:      load = RAM,      type = bss, define = yes;
}
FEATURES {
    CONDES: type = constructor,
            label = __CONSTRUCTOR_TABLE__,
            count = __CONSTRUCTOR_COUNT__,
            segment = ONCE;
}
